package com.duelup.app.ui.theme

import androidx.compose.ui.graphics.Color

// Vibrant Brand colors for Kids
val Primary = Color(0xFFFF5252) // Bubblegum Pink / Red
val PrimaryVariant = Color(0xFFFF8A80)
val Secondary = Color(0xFF00E5FF) // Cyan / Bright Blue
val SecondaryVariant = Color(0xFF84FFFF)
val Tertiary = Color(0xFFFFD700) // Sunbeam Yellow

// Semantic colors
val Success = Color(0xFF00E676) // Bright Green
val Error = Color(0xFFFF1744)   // Bright Red

// Bright Surface colors
val Background = Color(0xFFF2F9FF) // Very light bouncy blue background
val Surface = Color(0xFFFFFFFF)    // Pure white for cards to pop
val SurfaceVariant = Color(0xFFE3F2FD) // Light blue tint for unselected/secondary
val SurfaceLight = Color(0xFFFFFFFF)

// Text colors (Darker for readability on light backgrounds)
val OnBackground = Color(0xFF2C3E50) // Dark Slate
val OnSurface = Color(0xFF2C3E50)
val OnSurfaceVariant = Color(0xFF546E7A)
val OnPrimary = Color(0xFFFFFFFF)
val OnSecondary = Color(0xFFFFFFFF)

// Accent colors
val Gold = Color(0xFFFFD700)
val TimerWarning = Color(0xFFFFC107)
val TimerDanger = Color(0xFFFF5252)
val TimerSafe = Color(0xFF00E676)

// Difficulty colors
val DifficultyEasy = Color(0xFF00E676) // Bright Green
val DifficultyMedium = Color(0xFFFFC107) // Yellow Orange
val DifficultyHard = Color(0xFFFF5252) // Bright Red
